import { ResponseFormat, Manifest } from "../../utils/utils.ts";

export function getAppIndex(Reply: ResponseFormat): ResponseFormat {
	/* This function is used to read and give files
	of an application to the window in the Frontend */
	return Reply
}